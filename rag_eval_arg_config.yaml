dataset: trec-covid
profiles:
  - baseline

query_limit: 50
# true: drop + re-index each run. false: reuse collection when it already exists.
recreate: false
index_concurrency: 24
predict_question_max_concurrency: 15

k_values: [3, 10, 20]
chunk_fetch_multiplier: 4

pool:
  rel_threshold: 1
  max_distractors_per_query: null
